{
  "name": "orbital-command",
  "version": "0.2.0",
  "private": true,
  "description": "Unified operations, observability and automation platform.",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @orbital/api",
    "dev:api": "npm run dev -w @orbital/api",
    "dev:web": "npm run dev -w @orbital/web",
    "dev:gateway": "npm run dev -w @orbital/gateway",
    "build": "npm run build -w @orbital/contracts && npm run build -w @orbital/api && npm run build -w @orbital/gateway && npm run build -w @orbital/web",
    "typecheck": "npm run build -w @orbital/contracts && npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
